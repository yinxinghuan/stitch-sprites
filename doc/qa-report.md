# 《拆线精灵》42 关候选发布 QA

## 结论

- **机械与桌面视觉结论**：通过。42 关均有可完成路径；原 35 关编号不变，新增 7 幅拓扑挑战作品。高清源纹理、逐关调色、闭合裁切、图鉴、完成/失败、窄屏和外部访客状态均已覆盖。
- **当前发布风险**：无 P0/P1 已知缺陷。
- **理解边界**：`comprehension unverified`。自动化不能代替第一次接触玩家对“目标、反复动作、进展/失败”的复述。
- **设备边界**：仍需发布后在真实微信 WebView 和至少一台中低端手机确认音画同步与温控。

## 内容与素材验证

| 项目 | 结果 | 证据 |
|---|---|---|
| 关卡数量 | 42 | `generated-pattern-order.md`、`generated-patterns.ts` |
| 颜色排序 | 2→3→4→5→6，不回退 | 运行时 `validateLevels()`、全关截图标题 |
| 闭合主体/无飞地 | 通过生成器校验 | `level-square-audit/` 42 张独立 1:1 图 |
| 源图切分 | 使用密度谷，不按等宽硬切 | `recrop-source-audit.png` |
| 原生密度 | 按素材保留 4/5/7/8px 针距 | 生成器 `SOURCES` |
| 高清纹理 | 42 张透明纹理进入棋盘与图鉴 | `public/patterns/`、关卡/图鉴截图 |
| 被否决废稿 | 不在 `SOURCES`，不生成关卡 | 源映射与构建产物扫描 |

原 35 关的裁切结论继续成立。本轮重点检查第 36 花园提灯、第 39 环尾眠狐和第 42 玻璃花房：主体完整落在圆绷内，未被非等比拉伸，4px 原生针距的纤维和饱和度保留；最外三层的玩法改色仍保留源线的明暗和厚度。

## 机械验证

`_qa/solve-levels.ts` 独立回放所有关卡：

- 42/42 完整解通过。
- 选择次数最少 15、最多 37、平均 27.0。
- 针脚最少 1,172、最多 5,909。
- 第 7 关失败序列 `1,1,1,1,1`。
- 第 20 关失败序列 `1,1,1,1,1`。
- 第 35 关失败序列 `3,3,1,3,3`。
- 第 36–42 关标准解峰值等待依次为 `1,1,2,2,3,3,4`；选择次数分别为 `34,33,34,33,33,36,35`。
- 第 36–42 关均存在五次选择内的失败路径，且每关初始只有一个外露玩法色。
- 每关逐色线轴容量与逻辑针脚数完全一致。

页面启动时不再计算全关解。42 关的线轴、容量和权威解全部离线生成；旧运行时计算超过 30 秒的问题未重新引入。

## 视觉与流程证据

### 代表关卡与布局

- 原 1–35 关沿用上一轮全关截图证据。
- `topology-challenge-first-pass-level36-390x844.png`、`level39-390x844.png`、`level42-390x844.png`：新增低／中／最高等待档。
- `topology-challenge-first-pass-level42-320x568.png`：最密关窄屏。
- 断言：无横向溢出；四张首牌同尺寸。390px 下为 76×80px，320px 下为 62.39×65.67px。

### 六层后牌预览与五色样本

- `back-card-preview-first-pass-platform-layout-level27-390x844.png`、`320x568.png`：第 27 关首次改为每列最多显示六张后牌颜色窄带。
- `back-card-preview-recheck-platform-layout-level27-390x844.png`、`320x568.png`：同状态复验；六层色带均有独立位置和本关真实颜色，首牌继续保持 76×80px／62.39×65.67px，页面无横向溢出。
- `back-card-preview-recheck-external-guest-level27-390x844.png`：外部访客栏正常覆盖顶部扩展区，未挤压或破坏下方牌列。
- 第 27 关是正式五色压力样本：4035 针、28 次选择、标准解峰值等待 2，五步失败路径为 `2,2,2,2,2`。该项只证明题面具有可解性和机械压力；五色是否达到目标体验仍以当前真人试玩反馈为准。

### 主流程

- `flow-final-platform-layout-level1-initial-390x844.png`
- `flow-final-platform-layout-level1-after-first-action-390x844.png`
- `flow-final-platform-layout-level1-complete-390x844.png`
- `flow-final-platform-layout-level7-failed-390x844.png`
- `flow-final-platform-layout-level35-initial-320x568.png`
- `flow-final-external-guest-level1-390x844.png`

完成流程使用真实权威解，只在 QA 中压缩长动画计时；行走时序另以真实速度验证。失败流程使用真实错误选牌，没有直接注入阶段。

### 图鉴

- `topology-challenge-gallery-unlocked1-390x844.png`：初始仅第一关可选。
- `topology-challenge-gallery-unlocked42-bottom-390x844.png`、`320x568.png`：42 张全解锁后的新增关卡区。
- 断言：42 张、解锁数量正确、底部滚动两次采样不跳动、无横向溢出、缩略图保持圆框、可见按钮均不小于 44px。

## 动效、反馈与性能

`motion-polish-second-pass-0000..8200ms-390x844.png` 与 `motion-polish-narrow-second-pass-0000..8200ms-320x568.png` 在真实计时下覆盖放大后的精灵、190ms 排队出洞、地面步行、分批拆线、继续推进和加长线束异步回收。390×844 待拆数：

`1297 → 1297 → 1297 → 1296 → 1295 → 1293 → 1292 → 1290 → 1287 → 1286 → 1285 → 1282 → 1278 → 1275 → 1272 → 1267 → 1264`

结论：440ms 前没有提前拆线；约 660–880ms 出现首次真实结果；之后持续推进。两种视口均能看清个体出队间隔，精灵与线束使用不同回收时间线，3.2 倍半径的线身在入洞前保持可见。进入结算阶段时清除未结束的动态任务，避免角色残影堆积。

最密第 42 关，390×844、2× DPR、6× CPU 限速：

| 指标 | 结果 |
|---|---:|
| 输入事件派发 | 22.5ms |
| 首个可见响应 | 27.5ms |
| 平均帧间隔 | 8.91ms |
| p95 | 10.3ms |
| 最差帧 | 41.6ms |
| >50ms 帧 | 0 |

## 剩余真人测试

给 3–5 名未看过设计稿的中国用户直接打开第一关，不口述规则。第一次明显开路后询问：

1. 这一关最终要做什么？
2. 你接下来会反复看什么、点什么？
3. 怎样算进展，怎样会失败？

通过信号：至少 4 人能准确复述，并在第 3 关第一次自由选择时依据外层颜色而非随机尝试。否则优先调整外层提示与五槽表达，不继续增加新机制。

## 本轮视觉评分

按拓扑挑战首轮与同状态复验截图评分：层级 4.5、统一性 5、可读性 4.5、手感反馈 4、素材质量 5、响应式 4.5、完成度 4.5，平均 **4.6/5**。没有 P0/P1；当前保留的边界仍是新玩家理解证据，而不是画面或布局缺陷。
