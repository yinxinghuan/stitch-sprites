# 《拆线精灵》41 关重排 QA

## 结论

- **关卡结构**：通过。正式集合为 41 关，不为整数目标保留弱图；第 1 关瓢虫是唯一教学关，第 2 关起按真实堵塞规则失败。
- **内容构成**：7 幅用户确认保留作品、17 幅六色作品、17 幅七色作品。水晶蜗牛、缝纫机、收音机、园艺机器人和超画幅七色候选均未接入。
- **角色演出**：通过。精灵最小绘制半径从 8.6px 提升到 10.2px，约放大 19%；线束与拆针效果仍使用原半径，排队、行走和异步回收没有被角色放大破坏。
- **热修复范围**：第 5 关恢复为四种真实线色；右上重开必须恢复完整棋盘、初始牌堆和五个空槽。

## 机械验证

`npx tsx _qa/solve-levels.ts` 对正式 41 关逐关使用权威规则回放：

- 41/41 均存在零道具完整解。
- 单关选择次数 15–48，平均 34.6。
- 六色章标准解峰值等待达到 4；七色章主体达到 3–4。
- 第 24 关存在失败序列 `[2,2,2,2,2]`。
- 第 37 关存在失败序列 `[1,0,0,0,0]`。
- 第 38 关存在失败序列 `[0,0,1,1,1]`。

这些失败都发生在可解题面中，来自玩家对可见牌的排序选择；不存在“开局外层颜色没有可用首牌”的发牌死局。

## 存档迁移

`npx tsx _qa/progress-migration.ts` 通过。schema 升为 v4，仅将旧 2/4/5/9/13/20/27 关映射到新 1–7 关，并按新编号修正关卡编号分；旧中途局清空，淘汰关卡成绩不进入新进度。

## 视觉证据

- `doc/references/generated-pattern-grid-preview.png`：41 幅正式作品全量联系表。
- `doc/references/recrop-source-audit.png`：正式源图裁切与主体闭合审计。
- `doc/references/level-square-audit/`：每关独立 1:1 审核图。
- `_qa/ui/level-curve-final-level24-390x844.png`：六色高压关完整圆绷与方形源纹理裁圆。
- `_qa/ui/level-curve-final-level41-390x844.png`、`320x568.png`：七色终关双尺寸，无横向溢出或主体裁断。
- `_qa/ui/gallery-41-final-unlocked41-bottom-390x844.png`、`320x568.png`：41 张绣品册、底部滚动、圆形缩略图与未解锁锁形合同。
- `_qa/ui/expanded-sprite-platform-layout-0660ms-390x844.png`、`1120ms-390x844.png`：放大精灵的出队、行走与同时在场状态。

Playwright 断言覆盖：四张首牌同尺寸、最多六层后牌颜色带、无横向溢出、绣品册 41 张、底部滚动稳定、圆形缩略图裁切、可见控制不小于 44px。

## 动效时序

`capture-walk.mjs` 在 390×844 回放真实时间序列：首次可见拆线约 660ms，随后待拆针脚持续下降。放大后的角色仍按约 190ms 间隔排队出洞，没有瞬移；角色返程和线束回收继续使用两条独立时间线。

## 功能与性能回归

- `feature-regression.mjs`：通过。第 1 关首动只有一个可选线轴，第一次真实拆线后立即开放；稳定节点刷新恢复、重开还原完整棋盘/初始牌堆/空槽、双击不缩放、未解锁缩略图不泄露、图鉴滚动不跳、无平台时榜单不渲染、有平台时榜首头像与分数入口正常。
- 390×844、2× DPR、6× CPU 限速：第 24／41 关首个可见响应为 23.6／27.1ms，平均帧间隔 9.19／9.25ms，p95 为 15／10.3ms；两关各出现一帧冷启动 >50ms，没有形成连续卡顿。

## 构建与发布门禁

- `npm run build`：通过。
- 构建产物使用 `base: './'`；公开凭据扫描、API base 审计和自托管存储审计均通过。
- 发布仍须按同一 commit 完成自托管主站与 GitHub Pages 双部署验证，并在两端实际 bundle 中核对 `runRevision` 热修复标识。
- 新玩家理解仍标记 **comprehension unverified**；自动解题不能替代新玩家复述“目标、反复动作、进展/失败”三问。
